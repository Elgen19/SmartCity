<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.SmartCity" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <item name="android:statusBarColor">@color/secondary_color</item>
        <item name="android:buttonTint">@color/checkbox_selector</item>





    </style>
    <style name="CustomSpinnerStyle">
        <item name="android:textColor">#000000</item>  <!-- Set text color to black -->
        <item name="android:dropDownHeight">200dp</item> <!-- Optional: Set the dropdown height -->
        <item name="android:dropDownVerticalOffset">10dp</item> <!-- Optional: Offset the dropdown -->
    </style>
    <style name="CustomTabLayout" parent="Widget.Design.TabLayout">
        <item name="tabIconTint">@color/tab_icon_color_selector</item> <!-- Reference your color state list -->
    </style>

    <style name="Theme.SmartCity" parent="Base.Theme.SmartCity" />
</resources>